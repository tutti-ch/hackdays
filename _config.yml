title: tutti.ch Hackdays
email: marcello@tutti.ch

baseurl: "/hackdays"

markdown: kramdown

category-list: [whoami, hack]

plugins:
  - jemoji

defaults:
  - scope:
      path: "assets/images"
    values:
      image: true
